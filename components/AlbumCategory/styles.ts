import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        margin: 10
    },
    text: {
        color: 'white',
        fontSize: 30,
        fontWeight: 'bold'
    }
})

export default styles;